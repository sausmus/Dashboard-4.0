<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Inbox Assistant</title>
  <link rel="icon" type="image/png" href="bjh-logo.png">
  <style>
    :root {
      --bg:#f4f6f8; --card:#fff; --text:#202124; --muted:#5f6368;
      --border:#dadce0; --blue:#1a73e8; --blue-light:#e8f0fe;
      --red:#b3261e; --shadow:0 2px 8px rgba(0,0,0,.08); --radius:16px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;min-height:100%;font-family:Arial,Helvetica,sans-serif;background:var(--bg);color:var(--text)}
    body{min-height:100vh;display:flex;flex-direction:column}
    button,input{font:inherit}
    button{cursor:pointer}
    .topbar{
      display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:14px;
      min-height:78px;padding:12px 18px;background:#fff;border-bottom:1px solid var(--border);
      box-shadow:0 1px 4px rgba(0,0,0,.06);position:relative;z-index:3;
    }
    .left,.right{display:flex;align-items:center;gap:9px}
    .right{justify-content:flex-end}
    .back,.button{
      min-height:40px;display:inline-flex;align-items:center;justify-content:center;gap:7px;
      border:1px solid var(--border);border-radius:10px;padding:8px 13px;background:#fff;
      color:var(--text);font-size:13px;font-weight:700;text-decoration:none;
    }
    .back{color:var(--blue)}
    .button.primary{background:var(--blue);border-color:var(--blue);color:#fff}
    .button:hover,.back:hover{background:#f8faff}
    .button.primary:hover{filter:brightness(.97)}
    .brand{display:flex;align-items:center;justify-content:center;gap:10px;min-width:0;white-space:nowrap}
    .brand img{width:44px;height:44px;object-fit:contain}
    .brand img.right-logo{transform:scaleX(-1)}
    .brand h1{font-size:22px;margin:0;letter-spacing:-.35px}
    .frame-wrap{position:relative;flex:1;min-height:calc(100vh - 78px)}
    iframe{width:100%;height:calc(100vh - 78px);border:0;background:#f4f6f8;display:block}
    .setup{
      max-width:760px;margin:42px auto;padding:0 18px;
    }
    .card{background:var(--card);border-radius:var(--radius);box-shadow:var(--shadow);padding:28px}
    .setup-header{display:flex;align-items:center;gap:14px;margin-bottom:18px}
    .icon{width:52px;height:52px;border-radius:14px;background:var(--blue-light);color:var(--blue);display:grid;place-items:center;flex:0 0 auto}
    .icon svg{width:28px;height:28px;fill:none;stroke:currentColor;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}
    .card h2{margin:0 0 4px;font-size:22px}
    .card p{margin:0;color:var(--muted);font-size:14px;line-height:1.55}
    label{display:block;margin:22px 0 7px;color:var(--muted);font-size:12px;font-weight:800;text-transform:uppercase;letter-spacing:.6px}
    input{width:100%;height:44px;padding:9px 11px;border:1px solid var(--border);border-radius:9px;outline:none;color:var(--text)}
    input:focus{border-color:var(--blue);box-shadow:0 0 0 3px rgba(26,115,232,.14)}
    .actions{display:flex;justify-content:flex-end;gap:9px;flex-wrap:wrap;margin-top:14px}
    .help{margin-top:9px!important;font-size:12px!important}
    .error{display:none;margin-top:10px;color:var(--red);font-size:13px;font-weight:700}
    .error.show{display:block}
    .frame-fallback{
      position:absolute;right:16px;bottom:16px;z-index:2;background:rgba(255,255,255,.96);
      border:1px solid var(--border);border-radius:10px;padding:9px 11px;box-shadow:var(--shadow);
      color:var(--muted);font-size:11px;
    }
    .frame-fallback a{color:var(--blue);font-weight:800;text-decoration:none}
    @media(max-width:760px){
      .topbar{grid-template-columns:auto 1fr auto;min-height:66px;padding:9px 10px}
      .brand img{display:none}.brand h1{font-size:18px}.hide-mobile{display:none}
      iframe{height:calc(100vh - 66px)}.frame-wrap{min-height:calc(100vh - 66px)}
      .setup{margin:24px auto}.card{padding:20px}
    }
  </style>
</head>
<body>
  <header class="topbar">
    <div class="left"><a class="back" href="index.html">← <span class="hide-mobile">Dashboard</span></a></div>
    <div class="brand">
      <img src="bjh-logo.png" alt="BJH Bobcat">
      <h1>Inbox Assistant</h1>
      <img src="bjh-logo.png" class="right-logo" alt="BJH Bobcat">
    </div>
    <div class="right">
      <button id="openStandaloneButton" class="button hide-mobile" type="button" hidden>Open Standalone ↗</button>
      <button id="changeUrlButton" class="button" type="button" hidden>⚙</button>
    </div>
  </header>

  <main id="setupView" class="setup" hidden>
    <section class="card">
      <div class="setup-header">
        <div class="icon" aria-hidden="true">
          <svg viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="14" rx="2"></rect><path d="m4 7 8 6 8-6"></path></svg>
        </div>
        <div>
          <h2>Connect Inbox Assistant</h2>
          <p>Paste the deployed Google Apps Script web-app URL once. This browser will remember it.</p>
        </div>
      </div>
      <label for="webAppUrl">Apps Script Web App URL</label>
      <input id="webAppUrl" type="url" autocomplete="off" spellcheck="false" placeholder="https://script.google.com/macros/s/…/exec">
      <p class="help">Use the <strong>/exec</strong> URL from Deploy → Manage deployments. The URL stays in this browser's local storage; it is not committed to GitHub.</p>
      <div id="urlError" class="error"></div>
      <div class="actions">
        <button id="saveUrlButton" class="button primary" type="button">Save & Open Inbox Assistant</button>
      </div>
    </section>
  </main>

  <section id="frameView" class="frame-wrap" hidden>
    <iframe id="assistantFrame" title="Teacher Inbox Assistant" allow="clipboard-read; clipboard-write"></iframe>
    <div class="frame-fallback">If Google sign-in prevents embedding, <a id="fallbackLink" href="#" target="_blank" rel="noopener">open the app in a new tab</a>.</div>
  </section>

<script>
(() => {
  'use strict';
  const STORAGE_KEY = 'teacherDashboard.inboxAssistantUrl.v1';
  const setupView = document.getElementById('setupView');
  const frameView = document.getElementById('frameView');
  const webAppUrl = document.getElementById('webAppUrl');
  const urlError = document.getElementById('urlError');
  const saveUrlButton = document.getElementById('saveUrlButton');
  const changeUrlButton = document.getElementById('changeUrlButton');
  const openStandaloneButton = document.getElementById('openStandaloneButton');
  const assistantFrame = document.getElementById('assistantFrame');
  const fallbackLink = document.getElementById('fallbackLink');

  function normalizeUrl(value) {
    const text = String(value || '').trim();
    try {
      const url = new URL(text);
      if (url.protocol !== 'https:' || url.hostname !== 'script.google.com') return '';
      if (!url.pathname.includes('/macros/') || !url.pathname.endsWith('/exec')) return '';
      return url.href;
    } catch (_) { return ''; }
  }

  function showSetup(message = '') {
    const saved = localStorage.getItem(STORAGE_KEY) || '';
    webAppUrl.value = saved;
    setupView.hidden = false;
    frameView.hidden = true;
    changeUrlButton.hidden = true;
    openStandaloneButton.hidden = true;
    urlError.textContent = message;
    urlError.classList.toggle('show', Boolean(message));
    assistantFrame.removeAttribute('src');
  }

  function showFrame(url) {
    setupView.hidden = true;
    frameView.hidden = false;
    changeUrlButton.hidden = false;
    openStandaloneButton.hidden = false;
    assistantFrame.src = url;
    fallbackLink.href = url;
    openStandaloneButton.onclick = () => window.open(url, '_blank', 'noopener');
  }

  function saveUrl() {
    const url = normalizeUrl(webAppUrl.value);
    if (!url) {
      showSetup('Enter a valid deployed Apps Script /exec URL from script.google.com.');
      webAppUrl.focus();
      return;
    }
    localStorage.setItem(STORAGE_KEY, url);
    showFrame(url);
  }

  saveUrlButton.addEventListener('click', saveUrl);
  webAppUrl.addEventListener('keydown', event => {
    if (event.key === 'Enter') saveUrl();
  });
  changeUrlButton.addEventListener('click', () => showSetup());

  const saved = normalizeUrl(localStorage.getItem(STORAGE_KEY));
  if (saved) showFrame(saved); else showSetup();
})();
</script>
</body>
</html>
